/**
 * @param {number}    acc   Accumulated value of the reducer.
 * @param {number}    cur   Current value of the array.
 */
export const sumReducer = (acc: number, cur: number): number => cur + acc
